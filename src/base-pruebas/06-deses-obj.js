

export const usContext = ({ clave, nombre, anios, rango }) => {

    // console.log( nombre, edad, rango );
    
    return {
        clave: clave,
        nombre: nombre,
        anios: anios,
        rango: rango,
        latlng: {
            lat: 14.1232,
            lng: -12.3232
        }
    }

}
